import React, { Fragment } from "react";
import PropTypes from "prop-types";

import "./MapChart";
import MapChart from "./MapChart";

const Main = props => {
  return (
    <Fragment>
      <MapChart />
    </Fragment>
  );
};

Main.propTypes = {};

export default Main;
